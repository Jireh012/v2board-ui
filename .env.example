# 与后端 SM4_KEY 一致（16 字节 UTF-8 或 32 hex）
# 开发：复制为 .env.development
# 打包：复制为 .env.production 后执行 npm run build / docker build
VITE_SM4_KEY=0123456789abcdef

# 与后端 site.public_config_path 完全一致（首次启动面板后在「系统配置 → 站点」查看/生成）
VITE_PUBLIC_CONFIG_PATH=/c/devconfig
